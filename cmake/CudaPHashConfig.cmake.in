@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find the same dependencies that CudaPHash needs
find_dependency(CUDAToolkit REQUIRED)

# Include the exported targets
include("${CMAKE_CURRENT_LIST_DIR}/CudaPHashTargets.cmake")

check_required_components(CudaPHash)